{
  "name": "living-pitch",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit && vite build",
    "scan:smoke": "node --experimental-strip-types scripts/scan-smoke.ts",
    "preview": "vite preview",
    "dev": "vite"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2",
    "vite": "^6.0.5"
  }
}
