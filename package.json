{
  "name": "living-pitch",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "node --experimental-strip-types scripts/gen-static-routes.mjs && tsc --noEmit && vite build",
    "scan:smoke": "node --experimental-strip-types scripts/scan-smoke.ts",
    "share:smoke": "node --experimental-strip-types scripts/share-smoke.mjs",
    "summit:smoke": "node --experimental-strip-types scripts/summit-smoke.ts && node --experimental-strip-types scripts/cold-start-smoke.ts && node scripts/task-3-smoke.mjs",
    "preview": "vite preview",
    "dev": "vite"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2",
    "vite": "^6.0.5"
  }
}
